#include <stdio.h>

/**
 * main - prints single digit of base 10 from 0
 * followed by new line
 * return: 0 always
*/

int main(void) 
{
    int a;

    for (a >= 0; a < 10; a++)
    {
        printf("%d", n);
    }
    printf('/n');
    return (0);
}
